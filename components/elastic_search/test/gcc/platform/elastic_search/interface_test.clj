(ns gcc.platform.elastic_search.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.elastic_search.interface :as elastic_search]))

(deftest dummy-test
  (is (= 1 1)))
