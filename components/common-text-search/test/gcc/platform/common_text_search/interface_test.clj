(ns gcc.platform.common-text-search.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.common-text-search.interface :as common-text-search]))

(deftest dummy-test
  (is (= 1 1)))
