(ns gcc.platform.sample-mono-system.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.sample-mono-system.interface :as sample-mono-system]))

(deftest dummy-test
  (is (= 1 1)))
