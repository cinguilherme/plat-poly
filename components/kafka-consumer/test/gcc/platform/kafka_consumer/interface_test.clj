(ns gcc.platform.kafka-consumer.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.kafka-consumer.interface :as kafka-consumer]))

(deftest dummy-test
  (is (= 1 1)))
