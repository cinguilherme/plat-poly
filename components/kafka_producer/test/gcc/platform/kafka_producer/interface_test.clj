(ns gcc.platform.kafka-producer.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.kafka-producer.interface :as kafka-producer]))

(deftest dummy-test
  (is (= 1 1)))
