(ns gcc.platform.common-document.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.common-document.interface :as common-document]))

(deftest dummy-test
  (is (= 1 1)))
