(ns gcc.platform.common-metrics.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.common-metrics.interface :as common-metrics]))

(deftest dummy-test
  (is (= 1 1)))
