(ns gcc.platform.common-messaging.interface-test
  (:require [clojure.test :as test :refer :all]
            [gcc.platform.common-messaging.interface :as common-messaging]))

(deftest dummy-test
  (is (= 1 1)))
